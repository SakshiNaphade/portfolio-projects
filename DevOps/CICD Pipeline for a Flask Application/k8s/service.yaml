apiVersion: v1
kind: Service

metadata:
  name: flask-cicd-service

spec:
  selector:
    app: flask-cicd-demo

  type: NodePort

  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30007